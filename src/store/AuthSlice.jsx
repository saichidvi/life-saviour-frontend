import {createSlice} from "@reduxjs/toolkit";

const user = {
    name : "",
    age : "",
    email : ""
}

export const AuthSlice = createSlice({
    name : "",
    initialState : user,
    reducers : {
        
    }
})